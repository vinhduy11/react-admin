import { createTheme } from "@mui/material";

export const themeDark = createTheme({
  palette: {
    background: {
      default: "#222222",
    },
    text: {
      primary: "#000",
    },
  },
});
