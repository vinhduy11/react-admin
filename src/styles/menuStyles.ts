import { createTheme } from "@mui/material";

export const menuDarkTheme = createTheme({
  palette: {
    background: {
      default: "#497599",
    },
    text: {
      primary: "#fff",
    },
  },
});
